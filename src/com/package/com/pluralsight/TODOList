// ---------------------------------- Detail Tweaks --------------------------------------
// TODO: Add code block(s) for when user inputs wrong variable type

// ---------------------------------- LedgerManager --------------------------------------
// DONE: Create LedgerManagement class
// DONE: Declare transactions ArrayList
// DONE: Build constructor that calls loadTransactions()
// DONE: loadTransactions() — reads CSV line by line, split on "|", creates Transaction objects
// DONE: addTransaction() — BufferedWriter with "append mode (true)""
// DONE: printHeader() — header to use on all displays
// DONE: displayAll() — prints all transactions
// DONE: displayDeposits() — prints deposits (amount > 0)
// DONE: displayPayments() — prints payments (amount < 0)
// DONE: monthToDate() — filters by current month and year
// DONE: previousMonth() — filters using minusMonths(1)
// DONE: yearToDate() — filters by current year
// DONE: previousYear() — filters by current year - 1
// DONE: searchByVendor() — filters by vendor name using .contains


// ------------------------------------- Main ---------------------------------------------
// DONE: Setup main()
// DONE: main() calls homeScreen()
// DONE: homeScreen() — while loop with switch
// DONE: Create a LedgerManagement object at the top of the class to call within main()
// DONE: deposit() — prompt description, vendor, amount — use LocalDate/LocalTime — save via addTransaction()
// DONE: payment() — same as deposit() but amount stored as negative
// DONE: ledgerScreen() — while loop with switch, calls display methods and reportsScreen()
// DONE: reportsScreen() — while loop with switch, calls matching filter methods



// ---------------------------------- Transaction -----------------------------------------
// DONE: Created Transaction class
// DONE: Declared all five data members
// DONE: Generate constructors, getters, and setters
// DONE: Added toString()

